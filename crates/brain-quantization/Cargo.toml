[package]
name = "brain-quantization"
version = "0.2.0"
edition = "2021"
description = "Production-grade quantization, pruning, and sparse matrix engine for the Brain deep learning framework"
license = "MIT OR Apache-2.0"

[dependencies]
brain-core = { path = "../brain-core" }
brain-graph = { path = "../brain-graph" }
