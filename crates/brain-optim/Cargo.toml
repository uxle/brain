[package]
name = "brain-optim"
version = "0.2.0"
edition = "2021"
description = "Production-grade optimizer suite for the Brain deep learning framework"
license = "MIT OR Apache-2.0"

[dependencies]
brain-core = { path = "../brain-core" }
