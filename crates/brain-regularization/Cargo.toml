[package]
name = "brain-regularization"
version = "0.2.0"
edition = "2021"
description = "Production-grade regularization toolkit: dropout family, normalization layers, weight decay, and early stopping for the Brain deep learning framework"
license = "MIT OR Apache-2.0"

[dependencies]
brain-core = { path = "../brain-core" }
